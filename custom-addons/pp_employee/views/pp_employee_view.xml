<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
        <record model="ir.ui.view" id="pp_employee.list">
          <field name="name">pp_employee list</field>
          <field name="model">pp_employee.pp_employee</field>
          <field name="arch" type="xml">
            <tree>
              <field name="name"/>
              <field name="value"/>
              <field name="value2"/>
            </tree>
          </field>
        </record>
        -->

        <!-- actions opening views on models -->
        <!--
        <record model="ir.actions.act_window" id="pp_employee.action_window">
          <field name="name">pp_employee window</field>
          <field name="res_model">pp_employee.pp_employee</field>
          <field name="view_mode">tree,form</field>
        </record>
        -->

        <!-- server action to the one above -->
        <!--
        <record model="ir.actions.server" id="pp_employee.action_server">
          <field name="name">pp_employee server</field>
          <field name="model_id" ref="model_pp_employee_pp_employee"/>
          <field name="state">code</field>
          <field name="code">
            action = {
              "type": "ir.actions.act_window",
              "view_mode": "tree,form",
              "res_model": self._name,
            }
          </field>
        </record>
        -->

        <!-- Top menu item -->
        <menuitem name="Jumbo Employees" id="pp_employee.menu_root"/>
        <!-- menu categories -->
        <menuitem name="Jumbo Employees" id="pp_employee.menu_jumbo_employee" parent="pp_employee.menu_root"/>
        <menuitem name="Employees" id="pp_employee.menu_employees" parent="pp_employee.menu_jumbo_employee"/>
        <!-- actions -->
        <!--
        <record model="ir.actions.act_window" id="employee_list_action">
            <field name="name">openacademy course window</field>
            <field name="res_model">openacademy.course</field>
            <field name="res_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="context" eval="{'search_default_my_courses': 1}"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">Create the first course</p>
            </field>
        </record>
        -->

    </data>
</odoo>