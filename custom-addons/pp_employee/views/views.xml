<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="pp_employee.list">
      <field name="name">pp_employee list</field>
      <field name="model">pp_employee.pp_employee</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="pp_employee.action_window">
      <field name="name">pp_employee window</field>
      <field name="res_model">pp_employee.pp_employee</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="pp_employee.action_server">
      <field name="name">pp_employee server</field>
      <field name="model_id" ref="model_pp_employee_pp_employee"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="pp_employee" id="pp_employee.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="pp_employee.menu_1" parent="pp_employee.menu_root"/>
    <menuitem name="Menu 2" id="pp_employee.menu_2" parent="pp_employee.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="pp_employee.menu_1_list" parent="pp_employee.menu_1"
              action="pp_employee.action_window"/>
    <menuitem name="Server to list" id="pp_employee" parent="pp_employee.menu_2"
              action="pp_employee.action_server"/>
    -->
  </data>
</odoo>