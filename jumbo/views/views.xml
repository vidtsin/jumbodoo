<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="jumbo.list">
      <field name="name">jumbo list</field>
      <field name="model">jumbo.jumbo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="jumbo.action_window">
      <field name="name">jumbo window</field>
      <field name="res_model">jumbo.jumbo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="jumbo.action_server">
      <field name="name">jumbo server</field>
      <field name="model_id" ref="model_jumbo_jumbo"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="jumbo" id="jumbo.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="jumbo.menu_1" parent="jumbo.menu_root"/>
    <menuitem name="Menu 2" id="jumbo.menu_2" parent="jumbo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="jumbo.menu_1_list" parent="jumbo.menu_1"
              action="jumbo.action_window"/>
    <menuitem name="Server to list" id="jumbo" parent="jumbo.menu_2"
              action="jumbo.action_server"/>
    -->
  </data>
</odoo>